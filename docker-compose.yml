services:
  deepsearch:
    # Use locally built Jib image (build with: ./gradlew :presentation:jibDockerBuild --no-configuration-cache)
    image: deepsearch:latest
    container_name: deepsearch
    ports:
      - "8080:8080"
    env_file:
      - .env
